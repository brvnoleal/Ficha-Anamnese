Atualizar a URL do webhook em `src/components/MedicalForm.tsx` (linha 23) de `http://localhost:5678/webhook/damskiodonto` para `https://cross-population-parking-methodology.trycloudflare.com/webhook/damskiodonto`.

Essa URL é um túnel Cloudflare público, então funcionará para usuários externos enquanto o túnel estiver ativo.